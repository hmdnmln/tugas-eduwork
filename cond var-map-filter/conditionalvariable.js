const nilai = 50;

const hasil = nilai > 40 ? 'LULUS' : 'TIDAK LULUS';
console.log(hasil);

const kondisiAND = nilai < 90 && 'LULUS';
console.log(kondisiAND);

const kondisiOR = nilai > 90 || 'TIDAK LULUS';
console.log(kondisiOR);
